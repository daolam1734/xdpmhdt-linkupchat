# LinkUp Application Configuration Template

# --- Backend Config ---
# Secret key to sign JWT tokens. Generate a random one for production.
SECRET_KEY=7963d57b018599426f432742468a356220796b42b9d363d6f73e4811a4369f9e

# CORS Origins (comma separated)
BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:3000

# MongoDB Configuration
MONGODB_URL=mongodb+srv://<user>:<password>@cluster.mongodb.net/linkupchat?retryWrites=true&w=majority
MONGODB_DB=linkupchat

# AI Services API Keys
GOOGLE_API_KEY=your_google_ai_key_here
OPENAI_API_KEY=your_openai_key_here

# --- Frontend Build Config (Passed during docker build) ---
# Note: These values are baked into the frontend build.
# For local docker-compose, these are usually handled in docker-compose.yml args.
VITE_API_URL=/api/v1
# VITE_WS_URL=ws://localhost:8000/api/v1
